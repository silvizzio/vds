---
title: Spacing
description: The spacing scale and how it drives layout density.
status: stable
category: Foundations
---

## Overview

Replace this with the spacing foundation. Foundations render as a single
scrolling page, no tabs. Use `## ` headings for sections and the shared blocks
(`DoDont`, `Anatomy`, `PropsTable`) as needed.

## Tokens

Document the tokens here and reference the CSS variables defined in
app/globals.css so the docs and the product read from the same source.
